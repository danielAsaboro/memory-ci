import { BadgeCheck, ExternalLink, Fingerprint } from "lucide-react";
export function ProvenanceCard({ source, trust, signatureVerified, digest }: { source: string | null; trust: string; signatureVerified: boolean; digest: string }) { return <section className="evidence-card"><div className="mini-heading"><Fingerprint size={15} /><span>Provenance</span></div><dl><div><dt>Source</dt><dd>{source ?? "No source URI returned"} {source ? <ExternalLink size={11} /> : null}</dd></div><div><dt>Trust</dt><dd><span className={`trust-dot ${trust}`} />{trust}</dd></div><div><dt>Signature</dt><dd><BadgeCheck size={12} />{signatureVerified ? "verified" : "not verified"}</dd></div><div><dt>Digest</dt><dd className="mono">{digest}</dd></div></dl></section>; }
