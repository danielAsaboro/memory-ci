import { Clock3 } from "lucide-react";
import type { Agent } from "../../src/contracts/dashboard";
import { EmptyState } from "./async-state";
export function AgentRolloutTable({ agents }: { agents: Agent[] }) { return <section className="panel"><div className="panel-heading"><div><span className="eyebrow">Registered consumers</span><h2>Registered agents</h2></div><span className="readiness">{agents.length} registered</span></div><div className="agent-table">{agents.map((agent) => <div className="agent-row" key={agent.id}><span className="agent-icon"><Clock3 size={15} /></span><span><strong>{agent.name}</strong><small>{agent.lastReadAt ? `Last receipt ${new Date(agent.lastReadAt).toLocaleString()}` : "No read receipt reported"}</small></span><span><small>Namespaces</small><strong>{agent.namespaceIds.length}</strong></span><span><small>Reads</small><strong>{agent.reads}</strong></span><span className="tag">{agent.lastReadAt ? "Receipt recorded" : "Never read"}</span></div>)}{!agents.length ? <EmptyState title="No agents are registered" detail="Register an agent when lifecycle operations are enabled." /> : null}</div></section>; }
