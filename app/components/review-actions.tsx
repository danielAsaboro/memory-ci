"use client";
import { Check, ShieldX, X } from "lucide-react";
export function ReviewActions({ blocked = false }: { blocked?: boolean }) { return <div className="review-actions"><div><strong>{blocked ? "Promotion blocked" : "Read-only review evidence"}</strong><small>{blocked ? "Blocking evidence prevents promotion." : "Lifecycle decisions will be enabled with the mutation workflow."}</small></div><button className="button subtle" disabled><X size={14} />Reject</button><button className="button danger-button" disabled><ShieldX size={14} />Quarantine</button><button className="button primary" disabled aria-disabled><Check size={14} />Approve</button></div>; }
